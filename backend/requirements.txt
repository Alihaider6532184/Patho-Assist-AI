# =============================================================================
# Patho-Assist AI — Backend Dependencies
# =============================================================================
# All AI inference runs 100% locally via Ollama.
# No data ever leaves the host machine.
# =============================================================================

# --- Core Web Framework ---------------------------------------------------
fastapi==0.115.6                # High-performance async API framework
uvicorn[standard]==0.34.0       # ASGI server with hot-reload support
python-multipart==0.0.20        # Required for FastAPI file uploads (UploadFile)

# --- AI / LLM Orchestration -----------------------------------------------
langchain==0.3.14               # LLM orchestration & prompt management
langchain-community==0.3.14     # Community integrations (Ollama, ChromaDB)
langchain-text-splitters==0.3.4 # Text chunking utilities (RecursiveCharacterTextSplitter)

# --- Vector Database (Local, In-Process) -----------------------------------
chromadb==0.6.3                 # Embedded vector store — no external server needed

# --- Embeddings -----------------------------------------------------------
sentence-transformers==3.3.1    # Local sentence embeddings (all-MiniLM-L6-v2)

# --- Document Processing --------------------------------------------------
pypdf==5.1.0                    # PDF text extraction (pure Python, no system deps)

# --- Ollama Client (Local Mode) -------------------------------------------
ollama==0.4.7                   # Official Python client for Ollama REST API

# --- Llama API Client (Cloud Mode) ----------------------------------------
llama-api-client==0.6.0         # Official Meta Llama API SDK

# --- Utilities -------------------------------------------------------------
pydantic==2.10.4                # Data validation & settings (used by FastAPI)
pydantic-settings==2.7.1        # Environment-variable–driven config
python-dotenv==1.0.1            # .env file loader
Pillow==11.1.0                  # Image processing for vision pipeline
httpx==0.28.1                   # Async HTTP client for Ollama API (non-blocking)
